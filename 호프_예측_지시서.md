# 영화 '호프(HOPE)' 최종 관객수 예측 프로젝트 — Claude Code 구현 지시서

이 문서를 Claude Code에 그대로 전달하면 됩니다. 아래 요구사항을 순서대로 구현하세요.

---

## 0. 프로젝트 배경 및 목표

ATU Partners는 나홍진 감독 영화 '호프(HOPE)'에 투자한 재무적 투자자다 (기존 `hope-dashboard` 프로젝트 참고).
이번 프로젝트는 실시간 대시보드와 별개로, **호프의 최종 누적 관객수를 예측하는 분석 리포트**를
만드는 것이 목표다.

- 기준일: 오늘(문서 작성일) 기준 호프는 2026-07-15 개봉, 개봉 후 D+11일차
- 방법론: KOBIS 데이터로 확보한 **2023-01 ~ 2026-07 한국영화 누적 500만 명 이상** 흥행작들을
  비교군으로 삼아, 그 영화들의 "개봉 후 경과일 대비 최종 관객수 배수"와 "개봉 후 60일 일별 곡선
  패턴"을 분석한다.
- 이를 호프의 현재까지 실적에 적용해 **최종 관객수를 best / base / worst 3단계로 예측**하고,
  반응형 HTML 리포트로 시각화한다.
- 결과물은 **로컬 정적 리포트**다. 배포하지 않는다. 데이터 수집 스크립트가 만든 JSON 캐시를
  HTML이 읽어 렌더링하는 구조이며, 최신화하려면 수집 스크립트를 다시 실행한다.

이 프로젝트는 `hope-dashboard`와 완전히 분리된 새 프로젝트다. 폴더를 공유하지 않는다.

---

## 1. 데이터 소스 및 KOBIS API 제약 (반드시 먼저 이해할 것)

데이터 소스: 영화관입장권통합전산망(KOBIS) 오픈 API (https://www.kobis.or.kr/kobisopenapi)

**중요한 제약**: KOBIS 무료 API에는 "특정 영화 하나의 일자별 관객수 시계열"을 바로 주는 엔드포인트가
없다. 존재하는 것은 `boxoffice/searchDailyBoxOfficeList.json` — **특정 날짜의 박스오피스 순위
(기본 10위, `itemPerPage` 파라미터로 최대치 조정 가능)** 뿐이다. 따라서:

- 특정 영화의 일별 관객수를 얻으려면, 그 영화가 순위권에 들었던 모든 날짜의 박스오피스 응답을
  긁어모아 movieCd로 필터링해야 한다.
- 순위권 밖으로 밀려난 날(대개 개봉 후 30~60일 이후 저예산/저흥행작이거나, 대작이라도 극후반)은
  해당 영화의 그날 관객수가 **API로 확인 불가**하다. 이 결측을 리포트에 명시적으로 표기할 것
  (아래 5번 섹션의 "커버리지" 요구사항 참고).
- 이 결측은 배수(멀티플) 계산에는 큰 문제가 없다 — 앵커 시점(D+11일)과 최종 누적치는 대개 관측
  가능한 구간에 있거나, 최종 누적치는 영화 상세정보 API로 별도 확인 가능하다.

사용할 엔드포인트:
1. `boxoffice/searchDailyBoxOfficeList.json?key={KEY}&targetDt={YYYYMMDD}&itemPerPage=20`
   — 일별 박스오피스. `itemPerPage=20`으로 상위 20위까지 받아 커버리지를 넓힌다.
2. `movie/searchMovieList.json?key={KEY}&movieNm={명}&openStartDt=...&openEndDt=...`
   — 영화 기본정보(장르, 개봉일, 감독, 제작사, 국적) 조회. movieCd 확정에도 사용.
3. `movie/searchMovieInfo.json?key={KEY}&movieCd={CD}` — 영화 상세정보(장르, 제작국가 등 보강용).

API 키는 새로 발급받지 않는다. 기존 `hope-dashboard/.env`의 `KOBIS_API_KEY`를 그대로 재사용한다
(정상 작동 확인됨). 이 프로젝트의 `.env`에도 동일한 키를 복사해 넣거나, 상위 경로를 참조해도 된다.
**단, 이 프로젝트도 별도 git 저장소가 될 수 있으므로 `.env`는 반드시 `.gitignore` 처리한다.**

수집 기간: **2023-01-01 ~ 오늘**(약 1,300여 일). 일 API 호출 한도는 3,000회이므로 1회 실행으로
전체 수집이 가능하다. 다만 네트워크 오류 등에 대비해 **재실행 시 이미 수집된 날짜는 건너뛰는
idempotent 캐시 구조**로 설계한다 (날짜별 JSON 파일 또는 단일 JSON에 날짜 키로 저장, 이미 존재하면 skip).

---

## 2. 기술 스택 및 아키텍처

빌드 도구 없이 Node.js(내장 모듈만, 외부 의존성 최소화) + 정적 HTML/CSS/JS로 구현한다.
차트는 Chart.js(CDN)를 사용한다.

```
hope_expectation/
  collect.js          # KOBIS 일별 박스오피스 수집 → data/boxoffice-cache.json
  analyze.js           # 캐시로부터 비교군 추출, 배수·곡선 계산 → data/analysis.json
  data/
    boxoffice-cache.json   # 날짜별 원본 박스오피스 데이터 (gitignore 대상, 용량 큼)
    analysis.json           # 분석 결과 (index.html이 읽는 최종 산출물)
  index.html            # 반응형 리포트 (단일 파일, CSS/JS 인라인)
  .env                  # KOBIS_API_KEY (gitignore)
  .env.example
  .gitignore
  README.md             # 실행 방법, 데이터 최신화 방법, 방법론 요약
```

- `collect.js`: `node --env-file=.env collect.js` 로 실행. 2023-01-01부터 오늘까지 순회하며
  `searchDailyBoxOfficeList.json` 호출, 실패 시 재시도(최대 3회) 후 로그만 남기고 다음 날짜로
  진행. 요청 간 짧은 딜레이(예: 80~120ms)를 둬서 과도한 연속 호출을 피한다.
  기존 `hope-dashboard/lib/proxy-core.js`의 `fetchKobis` 패턴(파라미터 조립, JSON 파싱)을
  참고해도 좋다(복사해서 독립적으로 재작성, import는 하지 않는다 — 프로젝트 분리 원칙).
- `analyze.js`: `node analyze.js` 로 실행. 캐시를 읽어 아래 3번~4번 섹션의 로직을 수행하고
  `data/analysis.json`을 생성한다. 이 파일 하나만 `index.html`이 fetch한다.
- `index.html`: `data/analysis.json`을 fetch해서 렌더링. 로컬에서 `file://`로 열면 fetch가
  막히므로, 간단한 정적 서버(`node -e "require('http')..."` 또는 `npx serve` 등)로 구동하는
  방법을 README에 명시한다.

---

## 3. 비교군(유사 흥행작) 선정 및 가중치

`analyze.js`에서 수행:

1. 캐시된 모든 날짜의 박스오피스 레코드를 movieCd 기준으로 그룹핑, 각 영화의 관측된 최대
   `audiAcc`(누적관객수)를 구한다.
2. `audiAcc >= 5,000,000`인 movieCd를 후보로 추출한다.
3. 각 후보에 대해 `searchMovieList.json` 또는 `searchMovieInfo.json`으로 국적을 확인,
   **한국영화(`repNationNm` 또는 `nationAlt`가 한국)만 남긴다.**
4. 개봉일이 2023-01-01 ~ 오늘 범위인 것만 남긴다 (호프 자신은 비교군에서 제외).
5. **유사도 가중치**를 아래 기준으로 0~1 사이 값으로 산정해 각 영화에 부여한다 (여러 기준의 가중
   평균, 구체적 배점은 구현 시 합리적으로 정하되 아래 우선순위를 따를 것):
   - 장르 일치도: 스릴러/액션/SF 계열에 가까울수록 높은 가중치 (호프 장르: SF, 스릴러, 액션)
   - 개봉 규모 유사도: 개봉 첫 주 스크린 수가 호프와 비슷할수록 높은 가중치
   - 개봉 시기 유사도: 여름 성수기(6~8월) 개봉작에 높은 가중치
6. 결과를 `analysis.json`의 `comparableMovies` 배열에 저장 (영화명, movieCd, 개봉일, 장르,
   최종누적관객수, 유사도가중치, 데이터 커버리지 — 관측된 날짜 수 / 개봉후경과일).

---

## 4. 예측 로직 (하이브리드: 배수 예측 + 곡선 정합)

### 4-1. 배수(멀티플) 예측

- 앵커 시점 `D` = 호프의 최신 관측 가능한 "개봉 후 경과일" (오늘 기준 D+11 근방, 실행 시점의
  최신 KOBIS 데이터로 자동 결정 — 하드코딩 금지).
- 각 비교군 영화에 대해 배수 `r = 최종누적관객수 / (해당 영화의 D일차 누적관객수)`를 계산한다.
  D일차 데이터가 없는 영화(개봉 기간이 D일 미만이거나 결측)는 제외.
- 유사도 가중치를 반영한 **가중 배수 분포**를 만든다 (가중치만큼 해당 영화의 배수를 표본에
  복제하거나, 가중 백분위수 계산법 사용).
- 가중 분포에서 **P25 = worst, P50(중앙값) = base, P75 = best** 배수를 구한다.
  참고용으로 P10, P90도 함께 산출해 "극단적 범위"로 리포트에 표기한다.
- 최종 예측치 = 호프의 D일차 실제 누적관객수 × 각 배수.

### 4-2. 일별 곡선 정합 (시각화용)

- 비교군 중 유사도 가중치 상위 N개(예: 상위 8~10개, 데이터 커버리지가 충분한 것 위주)를 골라
  **개봉 후 경과일(1~60일) 대비 "당일 누적관객수 ÷ 최종누적관객수" 비율 곡선**을 구한다
  (모든 영화를 0~1 스케일로 정규화해 비교 가능하게 만듦).
- 호프의 실제 곡선(1~D일)과 이 비교군 정규화 곡선들을 겹쳐 그리면, 어떤 영화들과 궤적이
  비슷한지 시각적으로 드러난다.
- best/base/worst 각 최종 예측치에 평균 정규화 곡선 비율을 곱해 **호프의 60일 예상 궤적**
  (D+1일부터 D+60일까지)을 만들어 라인차트에 점선으로 이어 그린다.

`analysis.json` 구조 예시 (필드명은 구현 시 자유롭게 조정 가능, 의미만 유지):
```json
{
  "generatedAt": "2026-07-26T...",
  "hope": {
    "openDate": "2026-07-15",
    "anchorDay": 11,
    "anchorAudiAcc": 0,
    "dailySeries": [{"day": 1, "date": "2026-07-15", "audiCnt": 0, "audiAcc": 0}, "..."]
  },
  "prediction": {
    "worst": {"multiple": 0, "finalAudience": 0},
    "base": {"multiple": 0, "finalAudience": 0},
    "best": {"multiple": 0, "finalAudience": 0},
    "p10": {"multiple": 0, "finalAudience": 0},
    "p90": {"multiple": 0, "finalAudience": 0},
    "projectedTrajectory": [{"day": 12, "worst": 0, "base": 0, "best": 0}, "..."]
  },
  "comparableMovies": [
    {"movieNm": "...", "movieCd": "...", "openDt": "...", "genre": "...",
     "finalAudience": 0, "similarityWeight": 0, "coverage": 0, "multipleAtAnchorDay": 0}
  ],
  "normalizedCurves": [
    {"movieNm": "...", "curve": [{"day": 1, "ratio": 0}, "..."]}
  ]
}
```

---

## 5. 결과물(index.html) 요구사항

반응형(모바일 320px~데스크톱), Chart.js 사용, 단일 파일. 기존 `hope-dashboard`의 톤(블루/화이트
SaaS 스타일)을 따르되 새 프로젝트이므로 코드는 복사하지 말고 새로 작성한다.

포함할 요소:
1. **예측 요약 카드 3개** — worst / base / best 최종 관객수를 크게 표시, 각 카드에 적용 배수와
   근거(비교군 백분위) 명시.
2. **호프 실적 + 예측 궤적 라인차트** — 실선(실제 관측치, D일까지) + 점선(D+1~60일 예측,
   worst/base/best 3개 라인)을 한 차트에.
3. **비교군 정규화 곡선 오버레이 차트** — 상위 유사도 비교군들의 0~1 정규화 곡선을 흐린 색으로
   배경에 깔고 호프의 정규화 곡선을 굵게 강조.
4. **비교군 데이터 테이블** — 영화명, 개봉일, 최종 누적관객수, D일차 배수, 유사도 가중치,
   데이터 커버리지. 정렬 가능하면 좋음(필수는 아님).
5. **방법론 및 한계 설명 섹션** — 배수 예측법 요약, KOBIS 톱10/톱20 밖 결측치 한계, "예측치이며
   실제와 다를 수 있음" 명시적 디스클레이머.
6. 상단에 `data-generated-at`(analysis.json의 generatedAt) 표시 — 언제 기준 데이터인지 명확히.

---

## 6. 구현 순서

1. 폴더 구조 생성, `.env`/`.gitignore`/`.env.example` 설정 (KOBIS_API_KEY 재사용)
2. `collect.js` 작성 및 실행 — 2023-01-01~오늘 전체 수집, `data/boxoffice-cache.json` 생성
   (실행 시간이 길 수 있으므로 진행 상황을 콘솔에 로그. 중단 후 재실행해도 이어서 수집되는지 확인)
3. 수집 완료 후 캐시에서 500만+ 한국영화 후보 목록을 콘솔에 출력해 육안 검증 (호프 자신도
   목록에 나타나는지, 명백히 이상한 항목이 없는지 확인)
4. `analyze.js` 작성 — 비교군 확정, 가중치, 배수 분포, 정규화 곡선, 예측치 계산 →
   `data/analysis.json` 생성
5. 산출된 예측 수치가 상식적인 범위인지 자체 검토 (예: base 예측치가 호프 현재 누적치보다
   작으면 로직 오류)
6. `index.html` 작성 — 5번 섹션 요구사항 반영
7. 로컬 정적 서버로 구동, 브라우저(헤드리스 포함)로 반응형·차트 렌더링·콘솔 에러 0건 검증
8. `README.md` 작성 — 실행 방법, 데이터 최신화 방법(재수집), 방법론 한 문단 요약

---

## 7. 완료 기준 (체크리스트)

- [ ] `data/boxoffice-cache.json`에 2023-01-01~오늘 전체 날짜가 수집되어 있다 (결측 날짜가 있다면
      콘솔/README에 몇 건인지 명시)
- [ ] 500만 명 이상 한국영화 비교군이 최소 5편 이상 추출되었다 (너무 적으면 통계적 의미가 약함 —
      부족할 경우 500만 기준을 낮추기보다 기간·itemPerPage를 재검토)
- [ ] 각 비교군에 유사도 가중치와 D일차 배수가 계산되어 있다
- [ ] worst/base/best 3개 최종 관객수 예측치가 `analysis.json`에 존재하고, worst < base < best
      순서가 성립한다
- [ ] index.html이 정적 서버 구동 시 정상 로드되고, 반응형(모바일 뷰포트 포함)에서 레이아웃이
      깨지지 않는다
- [ ] 예측 궤적 라인차트, 비교군 정규화 곡선 차트, 비교군 테이블이 모두 실제 데이터로 렌더링된다
- [ ] 방법론/한계 섹션이 존재하고 KOBIS 데이터 결측 한계를 명시하고 있다
- [ ] 브라우저 콘솔 에러 0건 (헤드리스 브라우저로 실제 확인)
- [ ] `.env`가 커밋되지 않는다 (git 저장소로 만들 경우)
